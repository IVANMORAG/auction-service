# Puerto para Railway
PORT=3002

# MongoDB URI (Railway proporcionará esto)
MONGODB_URI=${MONGO_URL}

# Servicio de autenticación (tu servicio ya deployado)
AUTH_SERVICE_URL=https://auth-service-production-efff.up.railway.app

# JWT Secret (debe ser el mismo que tu auth-service)
JWT_SECRET=un_secreto_muy_fuerte_y_unico

# NTP Server
NTP_SERVER=pool.ntp.org

# CORS Origins (actualizar con tu dominio de frontend cuando lo tengas)
ALLOWED_ORIGINS=*

# Scheduler
SCHEDULER_ENABLED=true

# Bid Service URL (actualizar cuando deploys el bid-service)
BID_SERVICE_URL=https://bid-service-production-xxxx.up.railway.app

# Node Environment
NODE_ENV=production